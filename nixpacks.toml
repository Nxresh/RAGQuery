# Nixpacks configuration for Railway deployment
# Force Node.js as the primary provider (not Python)

[phases.setup]
nixPkgs = ["nodejs_20", "python3"]
aptPkgs = ["python3-pip"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
