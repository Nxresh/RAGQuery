apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ragquery
  labels:
    app: ragquery
    component: backend
spec:
  type: ClusterIP
  selector:
    app: ragquery
    component: backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
